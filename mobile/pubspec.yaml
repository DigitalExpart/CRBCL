name: crbcl_mobile
description: "CRBCL Family Wellness Case Management Platform — Field Worker Mobile Application"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  sqflite_sqlcipher: ^3.2.0
  flutter_secure_storage: ^9.0.0
  crypto: ^3.0.3
  http: ^1.2.0
  provider: ^6.1.0
  intl: ^0.19.0
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
